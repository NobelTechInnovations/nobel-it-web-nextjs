/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'images.unsplash.com',
      'randomuser.me',
      'tecnologia.vamtam.com'
    ],
  },
};

export default nextConfig;
